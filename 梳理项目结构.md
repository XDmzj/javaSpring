# IDEA项目目录结构
在 IDEA 中开发一个标准的 Java Web 项目（以 **Spring Boot + MyBatis** 为例），其目录结构遵循 Maven 的约定。这种结构被称为 **Standard Directory Layout**，它能让代码、配置和资源各归其位。

### 1. 顶层结构

当你打开 IDEA 的项目面板，你会看到以下核心组成部分：

- **`.idea/`**：IDEA 的私有配置文件（如窗口布局、断点信息）。不要手动修改，通常也不提交到 Git。
    
- **`src/`**：**源码根目录**（最重要的地方）。
    
- **`target/`**：项目编译后的输出目录（生成的 `.class` 文件和打包好的 `.jar` 或 `.war`）。
    
- **`pom.xml`**：Maven 的配置文件，定义了项目依赖（如 Spring、MyBatis、HikariCP）和插件。

### 2. `src` 内部的详细分层

`src` 目录分为 `main`（正式代码）和 `test`（测试代码）。

#### `src/main/java`（Java 源码区）

这里按照包名（Package）层层嵌套，通常包含我们之前讨论的四层架构：

- **`controller/`**：存放控制器类（如 `UserController.java`），处理 HTTP 请求。
    
- **`service/`**：
    
    - `UserService.java`（接口）
        
    - **`impl/`**：`UserServiceImpl.java`（具体业务逻辑实现）。
        
- **`mapper/`**（或 `dao/`）：MyBatis 的接口层，定义数据库操作方法。
    
- **`entity/`**（或 `model/`、`po/`）：对应数据库表的 Java 对象。
    
- **`dto/` / `vo/`**：用于前后端数据传输和展示的对象。
    
- **`config/`**：存放配置类（如 MyBatisConfig, SwaggerConfig 等）。
    
- **`Application.java`**：Spring Boot 的入口类（带 `@SpringBootApplication` 注解）。
    

#### `src/main/resources`（资源配置文件区）

- **`mapper/`**：存放 MyBatis 的 `*.xml` 映射文件。
    
- **`static/`**：存放前端静态资源（JS, CSS, Images），在前后端分离的项目中通常为空。
    
- **`templates/`**：存放后端渲染的模板文件（如 Thymeleaf），现在流行前后端分离，此处也常为空。
    
- **`application.yml`** (或 `.properties`)：项目的**核心配置文件**。这里配置数据库连接池（HikariCP）、MyBatis 扫描路径、端口号等。
    
### 3. 项目各层级协作逻辑

为了让你更清晰，我们模拟一个“查询用户信息”的请求是如何穿过这些文件夹的：

1. **前端请求**打到 `controller` 里的方法。
    
2. `controller` 调用 `service` 接口。
    
3. 程序进入 `service.impl` 执行业务逻辑（比如判断用户是否有权限）。
    
4. `service` 调用 `mapper` 接口。
    
5. MyBatis 自动去 `resources/mapper` 下找到对应的 `XML` 文件，通过 `DataSource` (HikariCP) 拿到的 `Connection` 去数据库执行 SQL。
    
6. 数据库返回的结果映射成 `entity` 对象，原路返回。
    
### 4. 关键文件对照表

|**目录/文件**|**作用**|**开发者关注点**|
|---|---|---|
|`pom.xml`|依赖管理|需要引入新功能（如 Redis）时修改|
|`application.yml`|全局环境配置|修改数据库地址、账号密码|
|`Mapper.java`|接口定义|定义 SQL 方法名|
|`Mapper.xml`|SQL 实现|编写具体的 SQL 语句和 ResultMap|
|`Service.java`|业务核心|处理事务控制、逻辑校验|

### 5. 补充：`.gitignore`

这是 Web 项目中不可忽视的文件，它告诉 Git 哪些文件（如 `target/` 文件夹、`.idea/` 文件夹）不需要上传到代码库，保持代码库的整洁。


---

# 一整个web项目的结构与技术栈


## 1. 表现层：用户看得到的地方 (Frontend & View)

虽然 Java 后端开发主要写逻辑，但必须了解数据是如何展示给用户的。

- **基础三剑客 (HTML/CSS/JS):** 所有的网页基石。
    
- **模板引擎 (Thymeleaf / JSP):**
    
    - **JSP:** 比较老旧的技术，将 Java 代码嵌入 HTML。
    
    - **Thymeleaf:** Spring Boot 官方推荐。它让 HTML 在没有服务器时也能正常预览，在运行时则由 Spring 动态替换其中的数据。
        
- **交互与异步 (AJAX / Axios):** 让你在不刷新页面的情况下，从前端调用 Spring 的接口。
    

## 2. 控制层与逻辑层：Spring 的核心 (The Glue)

这是你目前正在攻克的主战场。

- **Spring Framework:** 核心是 **IoC (控制反转)** 和 **AOP (面向切面编程)**。它决定了对象怎么创建、事务怎么控制。
    
- **Spring MVC:** 处理 Web 请求。它负责把用户发来的 URL 变成你 Java 方法里的参数。
    
- **Spring Boot:** “懒人版” Spring。它通过**自动配置**让你不再需要写成堆的 XML 配置文件。


## 3. 持久层：数据的归宿 (Data Persistence)

正如我们之前讨论的，这里负责把内存里的对象存入磁盘。

- **SQL (MySQL):** 基础的数据库查询语言。
    
- **连接池 (HikariCP / Druid):** 负责管理数据库连接，避免频繁开关连接导致的性能浪费。
    
- **ORM 框架 (MyBatis / MyBatis-Plus / JPA):** 把数据库表映射成 Java 类。你现在主攻 MyBatis，它让你能更精细地控制 SQL。
    


## 4. 构建与版本管理：项目的“后勤” (Tools)

这些工具虽然不直接写逻辑，但决定了项目能不能跑起来。

- **Maven / Gradle:** 依赖管理工具。你只需要在 `pom.xml` 里写个名字，它就会自动帮你从网上下载所有需要的 `.jar` 包。
    
- **Git:** 版本控制。你正在用的 GitHub 背后就是 Git，它记录了代码的每一次改动。



## 技术全景图：数据是如何流动的？

为了直观理解，你可以把一个请求想象成一次“快递派送”：

|**环节**|**对应技术**|**形象比喻**|
|---|---|---|
|**下单界面**|HTML / CSS / JS|快递 App 的用户界面|
|**提交订单**|AJAX / Axios|点击“下单”按钮，发送请求|
|**分拣中心**|**Spring MVC** (Controller)|识别包裹地址，分配给对应的快递员|
|**核心处理**|**Spring** (Service)|检查库存、计算价格、打包货物|
|**仓库取货**|**MyBatis / JDBC**|快递员去仓库（数据库）里翻找货物|
|**完成派送**|JSON / Thymeleaf|